<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>관리자 로그인</title>
  <link rel="stylesheet" href="/css/nav.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <%- include('partials/nav') %>

  <main class="page">
    <section class="card" style="margin-top:-24px;max-width:380px;margin-left:auto;margin-right:auto;">
      <h1 class="section-title">관리자 로그인</h1>

      <% if (error) { %>
        <p class="msg-error"><%= error %></p>
      <% } %>

      <form method="POST" action="/auth/login">
        <div class="form-group">
          <label>아이디</label>
          <input class="form-control" type="text" name="login_id" required />
        </div>

        <div class="form-group">
          <label>비밀번호</label>
          <input class="form-control" type="password" name="password" required />
        </div>

        <button type="submit" class="btn" style="width:100%;">로그인</button>
      </form>
    </section>

    <div class="footer">
      © <%= new Date().getFullYear() %> My Portfolio
    </div>
  </main>
</body>
</html>
